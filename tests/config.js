/*
* Used for testing purposes only. Driver configurations, especially those with apiKeys should
* be passed directly into the spiderCitations function by an external client.
*/
const config = {
  drivers: {
    scopus: {
      apiKey: '',
    }
  }
}

module.exports = config;